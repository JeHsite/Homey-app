# ביקורת אבטחה — Homey app
**תאריך:** 2026-09-15
**היקף:** `C:\Users\micha\Projects\Homey-app` — אפליקציית ניהול משק בית משפחתי, `index.html` (קובץ יחיד, React), Supabase (`fhmcalbsqgdghvbqucwd`, Postgres+RLS+Auth), 4 Edge Functions (`exchange-rate`, `market-news`, `send-reminders`, `weekly-review`), Vercel (סטטי).

**מתודולוגיה:** כל נקודה נבדקה בקוד (`Grep`/`Read`) ולא הונחה כברירת מחדל. בנוסף, כדי לוודא בפועל שההרשאות ברמת השורה (RLS) עובדות ולא רק "כתובות בקובץ SQL", נשלחו בקשות `curl` אמיתיות, לא מאומתות (עם מפתח ה-anon הציבורי בלבד, בלי התחברות), נגד **כל 13 הטבלאות** — כדי לבדוק אם משתמש לא-מזוהה יכול לקרוא נתונים. התוצאה מדווחת בנקודה 3.

---

## 1. פירוט לפי נקודה

### 1. קובץ סביבה או מפתחות בהיסטוריית הגיט
**תקין.** נבדק `git log --all --full-history` לקבצי `.env*` וקבצים בשם secret/credential — לא נמצא אף אחד, בעבר או היום. נבדקה גם היסטוריית `supabase/` לביטויי הקצאה כמו `CRON_SECRET = "..."` / `VAPID_PRIVATE = "..."` — אין.

### 2. מפתחות API שנחשפים לצד הלקוח
**תקין.** ב-`index.html:821-822` נחשף רק `SUPABASE_ANON_KEY` (מפתח `sb_publishable_...`) — זה בדיוק המפתח שמיועד לחשיפה בצד לקוח (מוגן ע"י RLS, לא ע"י סודיות המפתח). `HOMEY_VAPID_PUBLIC` (`index.html:33`) הוא מפתח **ציבורי** של VAPID (Web Push) — גם הוא מיועד לחשיפה מטבעו. לא נמצא אף מפתח `service_role`, `sb_secret`, `FINNHUB_KEY`, `ANTHROPIC_API_KEY` או `CRON_SECRET` בקוד הלקוח.

### 3. הרשאות ברמת השורה כבויות (RLS)
**תקין — מאומת בפועל, לא רק בקובץ.** כל הטבלאות שיש להן SQL בריפו (`exchange_rates`, `family_goals`, `personal_goals`, `profile_setup`, `kid_entries`, `kid_goals`, `profile_pins`, `push_subscriptions`, `weekly_reviews`) מפעילות `enable row level security` עם policies ממוקדות (`family_id = get_my_family_id()` או `owner_id = auth.uid()`).
הטבלאות הישנות יותר (`families`, `profiles`, `children`, `bills`, `transactions`, `notes`, `trips`, `gifts`, `savings_pots`, `assets`, `custom_categories`) קדמו לריפו הזה ואין להן קובץ SQL כדי לקרוא את ה-policy הטקסטואלי שלהן — אז נבדק **באופן אמפירי**: שליחת `GET` לכל אחת מ-13 הטבלאות דרך ה-REST API, עם מפתח ה-anon הציבורי בלבד, **בלי שום התחברות**. כולן החזירו `200 []` (בקשה תקינה, אפס שורות) — כלומר RLS פעיל ומדחה גישה לא-מזוהה בכל טבלה, כולל הישנות.

### 4. בדיקות הרשאה שמתבצעות בצד הלקוח בלבד
**בעייתי — בינוני.** `index.html:3273-3284` (`checkPin`): אם השאילתה הראשונה ל-`profiles` (העמודה `has_pin`, אחרי `pin.sql`) **נכשלת מכל סיבה שהיא** — לא רק "העמודה לא קיימת", אלא כל שגיאת רשת/RLS/זמינות — הקוד נופל חזרה לשאילתה הישנה שמביאה את `profiles.pin` (הטקסט הגלוי) לכל בני המשפחה, ומשווה אותו **בצד הלקוח**:
```js
// index.html:3276-3277
if (isDemo || p.pin) {
  if (pinInput === p.pin) passGate(p); else setPinError('קוד שגוי, נסה/י שוב');
  return;
}
```
זה בדיוק הדפוס שתוקן ב-`pin.sql` (ילד יכול לקרוא את קוד ה-PIN של ההורה, כי הוא נשלח כמו שהוא לכל מכשיר במשפחה). **בפועל היום זה לא מנוצל בפועל** — כי `pin.sql` כבר רץ, `profiles.pin` מאופס ל-NULL אצל כולם והטריגר `profiles_protect_pin` מוודא שהוא לא יתמלא שוב — כך שגם אם הקוד נופל למסלול הישן, `p.pin` תמיד ריק. אבל זה קוד מת ומסוכן שנשאר בפרודקשן: אם אי-פעם יחזור ערך לעמודה (רולבק, מיגרציה עתידית, שגיאת אדמין ב-SQL), ההגנה חוזרת מייד בלי אף שינוי קוד נוסף.

### 5. היעדר הגבלת קצב (rate limiting)
**תקין.** קוד ה-PIN חוסם אחרי 5 ניסיונות שגויים ל-5 דקות (`pin.sql`, `_homey_check_pin`) — מאומת בקוד. התחברות/הרשמה עוברות דרך Supabase Auth, שאוכפת הגבלת קצב ברמת הפלטפורמה (לא ניתן לבדוק את הערכים המדויקים מכאן, אבל זה חלק מובנה ולא אופציונלי ב-Supabase). שאר ה-Edge Functions (`weekly-review`, `send-reminders`) מוגנות מאחורי `x-cron-secret` — לא נגישות למשתמש קצה כלל, כך שאין להן חשיפה לניצול קצב.

### 6. שאילתות שנבנות בשרשור מחרוזות (SQL injection)
**תקין.** נסרק כל הריפו (`grep -rn "EXECUTE \|execute format\|query("`) — לא נמצאה בנייה דינמית של SQL. כל הגישה למסד היא דרך ה-query builder של supabase-js (`.eq()`, `.insert()`, וכו') או דרך פונקציות PL/pgSQL עם פרמטרים מוקלדים (`p_profile_id uuid`, וכו') — לא שרשור טקסט.

### 7. היעדר ולידציה של קלט בצד השרת
**אין מספיק מידע כדי לקבוע — חלקית.** הטבלאות עם SQL בריפו כן מאומתות ברמת ה-DB: `target > 0`, `amount >= 0`, `pin` בפורמט `^[0-9]{4,6}$` (נבדק בפונקציית `set_pin`, לא רק בלקוח). אבל הטבלאות הישנות (`transactions`, `bills`, `savings_pots`, `assets` וכו') אין להן DDL בריפו — אי אפשר לדעת מכאן אם יש `check()` constraints מקבילים, או שהולידציה שם היא **רק** מה שראיתי בקוד הלקוח (למשל `Number(f.target)`). מומלץ להריץ שאילתת אבחון (ראו בסוף) כדי לסגור את הפער הזה.

### 8. הזרקת HTML של משתמשים (XSS)
**תקין.** נסרק כל הקובץ ל-`dangerouslySetInnerHTML`/`.innerHTML =` — כל המופעים שנמצאו הם בתוך קוד הספרייה הארוז (React/Supabase JS, שורות ~148-368, מזוהה לפי אורך השורה והתוכן). לא נמצא אף שימוש ברמת קוד האפליקציה עצמה. React מבצע escaping אוטומטי לכל תוכן שמוצג דרך JSX/`createElement`.

### 9. סיסמאות שנשמרות בלי hash חזק
**תקין.** קודי PIN: `pgcrypto`, `bf` (bcrypt) — מאומת ב-`pin.sql:20,107`. סיסמאות החשבון עצמו מנוהלות במלואן ע"י Supabase Auth (bcrypt כברירת מחדל, לא בשליטת קוד האפליקציה) — לא נמצא אחסון מקביל/כפול של סיסמה בטבלאות האפליקציה.

### 10. טוקנים שנשמרים בדפדפן (localStorage) במקום עוגייה מאובטחת
**בעייתי — בינוני.** `sb.auth = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)` (`index.html:823`) בלי קונפיגורציית `storage` מותאמת — כך שברירת המחדל של supabase-js חלה: טוקן הגישה וה-refresh token נשמרים ב-`localStorage`, לא ב-cookie מסוג `httpOnly`. זה חושף אותם לגניבה **אם** יש XSS. **גורם ממתן:** לא נמצא אף וקטור XSS בקוד (ראו נקודה 8) — כך שהסיכון בפועל היום נמוך. התיקון המלא (cookie httpOnly) דורש שרת/proxy באמצע, שזה שינוי ארכיטקטורה משמעותי לאפליקציית SPA שמדברת ישירות עם Supabase — לא מוצע כתיקון מיידי, אלא כפריט לדעת עליו.

### 11. נתיבי אדמין בלי middleware שמאמת הרשאה
**לא רלוונטי.** אין באפליקציה שום מסך/נתיב אדמין — לא נמצא מושג "אדמין" בקוד האפליקציה עצמו (המופעים היחידים של המחרוזת "admin" הם בספריית הווendor).

### 12. הגדרות CORS מתירניות מדי
**תקין.** כל 4 הפונקציות מחזירות `Access-Control-Allow-Origin: "*"`. זה תקין כאן כי האימות הוא כולו מבוסס Header (Bearer/`apikey`/`x-cron-secret`), לא עוגייה — כלומר אין תרחיש CSRF שבו דפדפן "שולח קרדנציאלס אוטומטית" מכל מקור. Wildcard combined with credentialed cookies היה מסוכן; כאן זה לא המקרה.

### 13. הרשמה בלי אימות כתובת מייל
**תקין — מאומת בעקיפין דרך הקוד.** ב-`index.html:2093` יש טיפול ייעודי בהודעת השגיאה המדויקת שמחזיר Supabase Auth רק כאשר "Confirm email" מופעל וניסיון ההתחברות נדחה: `if (m.includes('email') && m.includes('confirm')) return 'יש לאשר את כתובת המייל לפני ההתחברות';`. קיום קוד שמטפל בדיוק בתרחיש הזה מעיד שהוא נבדק/נצפה בפועל — כלומר אימות מייל דלוק בפרויקט. (לא ניתן לאשר 100% בלי גישה להגדרות הדשבורד עצמן — אם תרצה ודאות מלאה: Authentication → Providers → Email → "Confirm email" צריך להיות ON).

### 14. גישה למשאבים לפי מזהה בלי בדיקת בעלות (IDOR)
**אין מספיק מידע כדי לקבוע — לגבי בידוד בין משפחות באמת.** כל ה-policies שכן נקראו מהקוד (`goals.sql`, `kids.sql`, `push.sql`) בודקים `family_id = get_my_family_id()` בצורה נכונה. ואומת אמפירית (נקודה 3) שגישה **לא-מזוהה** חסומה בכל טבלה. אבל לא נבדק אמפירית שמשתמש מזוהה ממשפחה A **לא** יכול לקרוא נתונים ממשפחה B — לבדיקה כזו צריך שני משתמשי-בדיקה אמיתיים בשתי משפחות שונות, שאין לי גישה אליהם. פונקציות ה-RPC (`verify_pin`, `set_pin`, `delete_my_account`) כן בודקות בעלות נכונה בקוד (`family_id = public.get_my_family_id()` / `auth.uid()`), וזה תקין.

### 15. לוגים ששומרים את גוף הבקשה המלא
**תקין.** בכל 4 הפונקציות, ה-`console.error` היחידים הם קודי שגיאה קצרים + הודעה (`console.error("push failed", code, e.message)`) — לא גוף בקשה מלא, לא סיסמה/טוקן/PII.

### 16. Webhooks בלי אימות חתימה
**לא רלוונטי.** אין באפליקציה שום נקודת קצה שמקבלת webhook מספק חיצוני (אין Stripe/תשלומים/אינטגרציה נכנסת).

### 17. פירוט שגיאות (stack trace) שחוזר למשתמש בפרודקשן
**תקין.** נמצאה פונקציית `friendlyAuthError` (`index.html:2088-2097`) שממפה הודעות שגיאה גולמיות מ-Supabase להודעות עבריות ידידותיות, עם ברירת מחדל גנרית ("משהו השתבש. נסו שוב בעוד רגע") לכל מה שלא זוהה. נסרק הקוד לחיפוש `setError(...message)` או הצגת `error.message` גולמי — לא נמצא אף מקום שמציג שגיאה גולמית למשתמש.

### 18. חבילות עם פרצות ידועות
**אין מספיק מידע כדי לקבוע.** אין `package.json`/lockfile לצד הלקוח (קובץ HTML יחיד) — אין מה להריץ עליו `npm audit`. ב-Edge Functions יש 3 תלויות דרך specifiers של Deno: `npm:@supabase/supabase-js@2` (**לא מוצמד** לגרסה מדויקת — "@2" צף על כל 2.x עתידי), `npm:web-push@3.6.7` (מוצמד), ו-`https://deno.land/std@0.168.0/http/server.ts` (מוצמד, אך גרסה ישנה יחסית של deno std). לא זמין לי כלי לסרוק CVEs בפועל נגד הגרסאות שנפרשו בפועל ב-Deno Deploy של Supabase.

### 19. היעדר דרישות חוזק לסיסמה
**תקין.** הרשמה: מינימום 8 תווים + חובה גם אותיות וגם ספרות (`index.html:2797-2802`), נבדק **לפני** הקריאה ל-`sb.auth.signUp`. PIN: פורמט `^[0-9]{4,6}$` נאכף גם בלקוח וגם בשרת (`set_pin` ב-`pin.sql:85-87`) — טוב יותר, כי זו אכיפה כפולה אמיתית.

### 20. העלאת קבצים בלי הגבלת סוג, גודל ושם
**בעייתי — בינוני.** תמונת פרופיל (`index.html:3551-3580`): הבדיקות (`file.type.startsWith('image/')`, גודל עד 4MB) הן **רק בצד לקוח**. הקובץ עובר קנבס שממיר אותו תמיד ל-JPEG (256×256), כך שאין סיכון XSS/הרצת קוד — אבל מי שקורא ישירות ל-REST API עם מפתח ה-anon (עוקף את קוד הלקוח לגמרי) יכול לשמור בעמודת `photo_url`/`avatar` כל מחרוזת שרוצה — לא בהכרח תמונה, בלי הגבלת אורך. אין `check()` constraint בטבלה שמגביל את זה. הסיכון מוגבל כי RLS עדיין דורש להיות בן המשפחה כדי לכתוב, כך שזה לא חור פתוח לאינטרנט — אבל שווה הגנה ברמת ה-DB.

---

## 2. טבלת סיכום

| # | נקודה | סטטוס | חומרה |
|---|---|---|---|
| 1 | קובץ סביבה/מפתחות בהיסטוריה | תקין | — |
| 2 | מפתחות API בצד לקוח | תקין | — |
| 3 | RLS כבוי | תקין (מאומת אמפירית) | — |
| 4 | הרשאה רק בצד לקוח | בעייתי | בינוני |
| 5 | היעדר rate limiting | תקין | — |
| 6 | SQL injection | תקין | — |
| 7 | ולידציה בצד שרת | אין מספיק מידע (חלקי) | — |
| 8 | XSS | תקין | — |
| 9 | סיסמאות בלי hash | תקין | — |
| 10 | טוקנים ב-localStorage | בעייתי | בינוני |
| 11 | נתיבי אדמין | לא רלוונטי | — |
| 12 | CORS מתירני | תקין | — |
| 13 | הרשמה בלי אימות מייל | תקין | — |
| 14 | IDOR | אין מספיק מידע (בידוד בין-משפחתי) | — |
| 15 | לוגים עם גוף מלא | תקין | — |
| 16 | Webhooks בלי אימות חתימה | לא רלוונטי | — |
| 17 | פירוט שגיאות בפרודקשן | תקין | — |
| 18 | חבילות עם CVEs | אין מספיק מידע | — |
| 19 | חוזק סיסמה | תקין | — |
| 20 | העלאת קבצים | בעייתי | בינוני |

**סיכום מספרי:** 12 תקין, 3 בעייתי (כולם בינוני, אף אחד קריטי/גבוה), 2 לא רלוונטי, 3 אין מספיק מידע (כולם ניתנים לסגירה — ראו "מה שנשאר לבדוק" בסוף).

---

## 3. רשימת תיקונים לפי סדר עדיפות

1. **#4 — הסרת מסלול ה-PIN הישן** (בינוני, אבל הכי קל וזול לתקן — למה שקוד מת יישאר?).
2. **#20 — הגבלת אורך `photo_url`/`avatar` ברמת ה-DB**.
3. **#10 — לתעד כסיכון מקובל** (אין תיקון קוד סביר בלי לשנות ארכיטקטורה; לוודא שאין רגרסיה בנקודה 8 בעתיד).
4. **לסגור את הפערים באין-מספיק-מידע** — להריץ את שאילתת האבחון בסוף המסמך (נקודות 7, 14, 18).

---

## 4. קוד מתוקן לשלושת הממצאים הבעייתיים

### תיקון #4 — הסרת מסלול ה-PIN הישן (`index.html`)

**לפני** (`checkPin`, ~שורה 3273):
```js
const checkPin = async () => {
  const p = pinGateFor;
  // דמו, או מסד שעוד לא עבר ל-pin.sql — השוואה מקומית כמו קודם
  if (isDemo || p.pin) {
    if (pinInput === p.pin) passGate(p); else setPinError('קוד שגוי, נסה/י שוב');
    return;
  }
  const res = await checkPinServer(p.id, pinInput);
  ...
```

**אחרי** — משאירים רק את מסלול הדמו (לגיטימי, אין מסד אמיתי בדמו), מסירים את ה-`|| p.pin`:
```js
const checkPin = async () => {
  const p = pinGateFor;
  if (isDemo) {
    if (pinInput === p.pin) passGate(p); else setPinError('קוד שגוי, נסה/י שוב');
    return;
  }
  const res = await checkPinServer(p.id, pinInput);
  ...
```
וב-`useEffect` שטוען את הפרופילים (~שורה 3195), אחרי ש-`pin.sql` כבר רץ בפרודקשן, אפשר גם להסיר את הנפילה החוזרת לעמודת `pin` הישנה:
```js
// לפני
sb.from('profiles').select('id, full_name, avatar, has_pin, status, photo_url').eq('family_id', familyId)
  .then(r => r.error ? sb.from('profiles').select('id, full_name, avatar, pin, status, photo_url').eq('family_id', familyId) : r)
// אחרי
sb.from('profiles').select('id, full_name, avatar, has_pin, status, photo_url').eq('family_id', familyId)
```
כך שגם אם השאילתה נכשלת, היא לא תנסה לשלוף PIN גלוי כגיבוי.

### תיקון #20 — הגבלת אורך תמונת פרופיל ברמת ה-DB (SQL חדש)
```sql
-- להריץ פעם אחת ב-SQL Editor. 200,000 תווים ≈ נדיב בהרבה מ-JPEG 256x256 בבסיס 64 (בערך 100-150KB בפועל)
alter table public.profiles
  add constraint profiles_photo_url_len check (photo_url is null or length(photo_url) < 200000);
alter table public.children
  add constraint children_photo_url_len check (photo_url is null or length(photo_url) < 200000);
```

### תיקון #10 — טוקנים ב-localStorage
אין שינוי קוד מוצע כרגע — זו החלטת ארכיטקטורה (הוספת שרת-ביניים כדי לתמוך ב-httpOnly cookies), לא תיקון נקודתי. הפריט נשאר מתועד; המיטיגציה בפועל היא לשמור על 0 וקטורי XSS (נקודה 8) — כל שינוי עתידי שמוסיף `innerHTML`/`dangerouslySetInnerHTML` הופך את הפריט הזה למסוכן בפועל, לא רק תיאורטי.

---

## 5. תוספת: דרישות רגולציה ישראליות לעוסק מורשה

**זהו צ'קליסט אינפורמטיבי בלבד, לא ייעוץ משפטי** — מומלץ לוודא חתימה סופית מול עורך/ת דין, ולגבי נגישות — מול יועץ/ת נגישות מוסמכ/ת, לפני השקה מסחרית.

### נגישות
- ✅ **הצהרת נגישות מפורסמת** קיימת (`index.html`, `LegalDoc` דרך `doc==='a11y'`), מפנה לתקן 5568/WCAG 2.0 AA, כוללת תאריך עדכון ופרטי קשר לרכז הנגישות.
- ⚠️ ההצהרה עצמה אומרת בפירוש: **"עדיין לא בוצעה בדיקת נגישות מלאה על ידי מורשה נגישות"** — זה בדיוק סעיף #13 ברשימת ה-14 שלך (ביקורת נגישות מלאה), עדיין פתוח. חוק שוויון זכויות לאנשים עם מוגבלות ותקנות ההנגשה עשויים לחייב בדיקה בפועל (לא רק הצהרה), תלוי בהיקף השימוש/סוג השירות — כדאי לברר מול יועץ נגישות אם יש סף שחל כאן.
- לא נבדק במסגרת ביקורת זו: ניגודיות צבעים בפועל, ניווט מקלדת מלא, תאימות לקורא מסך — זה בדיוק מה שהביקורת המלאה (#13 שלך, שמופיע גם כ-#6 בתת-הפרויקט) אמורה לכסות.

### פרטיות (חוק הגנת הפרטיות, כולל תיקון 13)
- ✅ מדיניות פרטיות מפורטת ומעודכנת (13.09.2026) קיימת ונגישה משני מקומות (מסך התחברות + טאב עזרה).
- ✅ מפרטת בסיס איסוף, מטרות, צדדים שלישיים (Supabase/Vercel/Finnhub/Anthropic) **כולל מיקום שרתים** (סיאול, ארה"ב לסקירה השבועית) ואת ההעברה מחוץ לישראל.
- ✅ מתייחסת לזכויות עיון/תיקון/מחיקה, כולל מחיקת חשבון עצמאית.
- ✅ מצהירה במפורש: אין עוגיות פרסום, אין מעקב/אנליטיקס — לכן לא נדרש באנר הסכמת עוגיות (אין עוגיות לא-חיוניות לתת עליהן הסכמה).
- ⚠️ לא נבדק אם יש חובת **רישום מאגר מידע** — תלוי בהיקף המשתמשים ובסוג המידע (המדיניות עצמה מציינת "מידע בעל רגישות מיוחדת" למידע פיננסי) — שווה בדיקה מול עו"ד אם ואיפה חוצים את הסף שמחייב רישום.

### הגנת הצרכן / מסחר אלקטרוני
- **לא רלוונטי כרגע** — השירות מוצהר כ"ללא תשלום" בתנאי השימוש ובמדיניות הפרטיות, אין רכישה/עסקה באתר. אם וכאשר יתווסף תשלום (ראו סעיף #14 ברשימה שלך — מוכנות לחנויות אפליקציות/מודל עסקי), יהיה צורך להוסיף: גילוי נאות מלא, זכות ביטול עסקה, ומדיניות תשלומים.

### פרטים מזהים בפוטר / גילוי נאות
- ✅ שם המפעיל ("איתמר הורביץ, ENT code, video & beyond") ואימייל ליצירת קשר מופיעים במדיניות ובתנאי השימוש.
- ⚠️ **לא נמצא מספר עוסק מורשה/ח.פ.** בשום מקום בקוד. מכיוון שהשירות כרגע חינמי (אין עסקה מסחרית), זו כנראה לא דרישה מחייבת היום — אבל אם יתחיל תשלום, זו תהפוך לדרישת גילוי נאות בסיסית.

---

## מה שנשאר לבדוק כדי לסגור את שלוש נקודות "אין מספיק מידע"

אם תרצה לסגור את הפערים (7, 14, 18) לגמרי, שתי דרכים:

**א. הריצי שאילתה אחת ב-SQL Editor** (סוגרת 7 חלקית — constraints על הטבלאות הישנות):
```sql
select table_name, column_name, is_nullable, data_type, character_maximum_length
from information_schema.columns
where table_schema = 'public'
  and table_name in ('transactions','bills','savings_pots','assets','trips','gifts','notes')
order by table_name, ordinal_position;

select conname, conrelid::regclass as table_name, pg_get_constraintdef(oid)
from pg_constraint
where connamespace = 'public'::regnamespace and contype = 'c'
order by table_name;
```
הדביקי לי את התוצאה ואוכל להשלים את נקודה 7 במלואה.

**ב. נקודה 14 (בידוד בין משפחות)** דורשת שני משתמשי-בדיקה אמיתיים בשתי משפחות שונות — לא משהו שאפשר לבדוק בלי חשבונות אמיתיים; אם יש לך חשבון בדיקה שני, אפשר לתאם בדיקה ידנית.

**ג. נקודה 18** תישאר "אין מספיק מידע" באופן מובנה — Deno Deploy לא חושף lockfile לבדיקה מקומית; הפתרון המעשי היחיד הוא להצמיד גרסה מדויקת ל-`@supabase/supabase-js` (למשל `@2.45.4` במקום `@2`) כדי שלפחות תדע בדיוק מה רץ, ולעקוב מדי פעם אחרי ה-changelog/advisories שלו.
