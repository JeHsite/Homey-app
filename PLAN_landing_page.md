# תוכנית: אתר נחיתה שיווקי ל-Homey

**סטטוס:** מאושר לביצוע (ראיון הושלם, שאלה 1). לא בוצע עדיין.
**נועד ל:** מר הורביץ (Etamar Horvitz), Homey app — `C:\Users\micha\Projects\Homey-app`.

---

## 0. מה זה כולל, ומה לא

**בפנים:**
1. עמוד נחיתה שיווקי סטטי אחד (עברית, RTL), שמסביר מה זה Homey, איזו בעיה הוא פותר, ומה יש בו — עם צילומי מסך אמיתיים מהאפליקציה.
2. שני כפתורי קריאה לפעולה: "הצטרפו בחינם" (מוביל למסך ההרשמה האמיתי) ו"נסו בלי הרשמה" (מוביל ישר למצב הדמו הקיים — **מנגנון שכבר קיים באפליקציה**, ראו L5 המתוקן).
3. פוטר עם גילוי נאות בסיסי (שם העסק, הבהרה לגבי סטטוס רישוי, הצהרת נגישות ראשונית) — לפי [[reference_israeli_site_launch_checklist]] של מר הורביץ.

**תיקון חשוב אחרי ביקורת:** הטיוטה הראשונה של התוכנית הניחה בטעות שצריך להוסיף קישור-ישיר-לדמו לאפליקציה. **זה כבר קיים** (`index.html:4239-4256`, פרמטר `?demo=parent`/`?demo=kid`, עם תמיכה גם ב-`&tab=` לקפיצה ללשונית ספציפית) — הקוד כולל אפילו הערה מפורשת "לצילומי מסך ולהצגת האפליקציה". **אין שום עריכה נדרשת ב-`index.html` בתוכנית הזו כלל.** כל הסעיפים למטה עודכנו בהתאם.

**בחוץ (לא בסקופ, מוצהר):**
- דומיין מותאם אישית (custom domain) — נשארים על כתובת Vercel הקיימת בינתיים, לפי בקשת המשתמש ("כרגע").
- גרסה באנגלית.
- טפסי לידים/ניוזלטר, כל איסוף מייל/פרטים בעמוד הנחיתה עצמו (העמוד לא אוסף שום מידע אישי — נמנעים מהצורך במדיניות פרטיות מלאה בשלב זה).
- אנליטיקס/פיקסלים של פרסום.
- שינוי בתמחור/ב-Cardcom/בתהליך התשלום עצמו.
- ביקורת נגישות פורמלית (נשאר פריט פתוח, כמו באפליקציה הראשית).

---

## 1. Assumptions Ledger

| # | החלטה | תיוג |
|---|---|---|
| L1 | מיצוב: מוקדמים (early-stage), חינמי במרכז, פרימיום מתויג "בקרוב" בלי מחירים, CTA ראשי = "הצטרפו בחינם" ולא "שלמו". | **(user)** — נבחר בראיון |
| L2 | **ארכיטקטורה (הוחלט על ידי, לפי בקשתך המפורשת):** קובץ HTML חדש ונפרד (`landing.html`) באותו repo ובאותו פרויקט Vercel — לא פרויקט/דומיין חדש. הסיבה: האפליקציה כבר static, בלי build step, פרוסה כ-Vercel project בודד; קובץ נפרד הוא הכי זול לתחזוקה ופריסה זהה (git push אחד). | (user delegated) + `[assumed: בסיס — if wrong: קל להפוך לפרויקט נפרד בהמשך]` |
| L3 | **הדומיין הראשי (`/`) נשאר האפליקציה, לא עמוד הנחיתה.** עמוד הנחיתה יעלה בנתיב נפרד (`/landing.html`, וכתובת ידידותית כמו `/start` אם תרצה rewrite ב-Vercel בהמשך). הסיבה: המשפחה שלך (ואולי בני משפחה נוספים) כבר משתמשים באפליקציה דרך הדומיין הראשי היום, כולל PWA מותקן עם `start_url: "./"` (verified: manifest.json) — אם `/` יהפוך לעמוד שיווקי, כל מי שכבר משתמש באפליקציה "יינעל בחוץ" מהקיצור שלו. זו נקודה שהחלטתי בעצמי כברירת מחדל בטוחה וזולה-להחזרה, ולא שאלתי עליה כי הסיכון חד-כיווני (קל לשנות אחר כך, קשה לתקן פגיעה במשתמשים קיימים). | `[assumed: default בטוח — if wrong: אפשר בעתיד להחליף מי יושב ב-'/' דרך vercel.json rewrite]` |
| L4 | תוכן חזותי = **צילומי מסך אמיתיים** מהאפליקציה (מצב דמו), לא מוקאפים מצוירים ולא iframe חי מוטמע. הסיבה: האפליקציה כבר כוללת מצב דמו מלא ועובד ("כניסת דמו מהירה", בלי הרשמה) — צילום מסך אמיתי זול, אמין, ותמיד משקף את המוצר בפועל. | `[assumed: אין חלופה תחרותית סבירה — if wrong: להחליף בעיצוב גרפי]` |
| L5 | **תוקן אחרי ביקורת.** קישור-ישיר-לדמו **כבר קיים** ב-`index.html` (verified: index.html:4239-4256, בתוך ה-`useEffect` הראשי של `App()`, **לא** ב-`LoginScreen`) — פרמטר `?demo=parent` (הורה) או `?demo=kid` (ילד), עם `&tab=<id>` אופציונלי שקופץ ללשונית ספציפית (`dashboard`/`bills`/`savings`/`investments`/`kidmoney` וכו', verified: index.html:1635-1658). המנגנון מריץ `setFamilyId('DEMO')` ישר בתוך `App()`, לפני שה-`authStage==='login'` בכלל נבדק — לכן כל תוספת בתוך `LoginScreen` (כמו הטיוטה הקודמת של הסעיף הזה) לעולם לא הייתה יכולה לרוץ, כי `LoginScreen` לא היה מגיע לרינדור. **אין צורך בשום שינוי קוד.** כפתור "נסו בלי הרשמה" בעמוד הנחיתה מקשר ישירות ל-`index.html?demo=parent`. | `(verified: index.html:4239-4256, 1635-1658)` |
| L6 | עיצוב: **`landing.html` מקבל CSS עצמאי ומינימלי משלו, נכתב מאפס** — לא "מעתיק" קלאסים כמו `ent-card`/`ent-pill-btn` מהאפליקציה. הסיבה (תוקן אחרי ביקורת): קלאסים כאלה תלויים בגיליון Tailwind המקומפל שמוטמע בתוך `index.html` (verified: index.html:419, `/*! tailwindcss v3.4.19 */`, ~2000 מחלקות utility) ובבלוק CSS מותאם נוסף (`.ent-card` לבדו מוגדר/מורחב ב-4 מקומות שונים: index.html:499, 509, 529, 588). העתקת שמות המחלקות בלי הגיליון המלא תיתן HTML לא מעוצב לגמרי. במקום זה: **ערכי הטוקנים בלבד** משמשים כהשראה עיצובית — צבעים מה-theme `ent` (verified: index.html:425-426 — נייבי `#0A0D13` / ברונזה `#CBA05A` / ציאן `#4FD8FF`, `--line: rgba(203,160,90,.16)`) וטוקני טיפוגרפיה/רדיוס (verified: index.html:784 — `--font-ui: 'IBM Plex Sans Hebrew'`, `--r-sm/md/lg`), אותו קישור Google Fonts בפועל (verified: index.html:16). `landing.html` כותב CSS מלא משל עצמו (flex/grid/spacing/כרטיסים/כפתורים) בעזרת הערכים האלה — לא מסתמך על אף class שמוגדר רק בתוך `index.html`. | `[assumed: תיקון אחרי ביקורת — if wrong: להשקיע בהעתקת הגיליון המלא, יקר ומיותר לעמוד סטטי אחד]` |
| L7 | פוטר: קרדיט עסקי זהה למה שכבר קיים באפליקציה (הביטוי המלא "Homey פותחה על ידי איתמר הורביץ (ENT code, video & beyond)" מורכב מ-`LEGAL_OWNER` שמוגדר ב-index.html:2746 ומורכב בפועל ב-index.html:7953), עם משפט הבהרה שהעסק בהליכי רישום (עוסק פטור) וללא גביית תשלום פעילה כרגע. אין קישור "חי" לתנאי שימוש/פרטיות של האפליקציה כי אלה כרגע מודלים בתוך React ולא כתובות URL עצמאיות (verified: index.html:3322 — `setLegalDoc('terms')`, state פנימי, לא ניתן לקשר אליו מבחוץ בלי לפתוח את האפליקציה) — לכן הפוטר יכיל טקסט גילוי נאות קצר ועצמאי במקום, עם **משפט שחלקו עטוף פיזית ב-`<a href="index.html">`** שמפנה לאפליקציה (איפה שאפשר לקרוא את התנאים המלאים לפני הרשמה — ראו נוסח מדויק בסעיף 3, "פוטר"). | `[assumed: פתרון סביר לבעיה טכנית אמיתית — if wrong: לבנות עמודי terms/privacy נפרדים]` |
| L8 | אין טופס איסוף מייל/ליד בעמוד הנחיתה עצמו — כל "פעולה" היא ניווט לאפליקציה (הרשמה אמיתית או דמו). זה פוטר את העמוד מהצורך במדיניות פרטיות נפרדת (לפי §2 ב-[[reference_israeli_site_launch_checklist]] — פרטיות נדרשת רק כשנאסף מידע). | `[assumed: הסבירות שתרצה טופס לידים בהמשך גבוהה — if wrong: להוסיף בהמשך, כולל מדיניות פרטיות]` |
| L9 | ביקורת נגישות פורמלית **לא** מבוצעת כעת (כמו באפליקציה הראשית) — רק עקרונות יסוד: alt לתמונות, מבנה כותרות היררכי (h1→h2→h3), ניגודיות צבעים מהטוקנים הקיימים (כבר נבדקו במסגרת האפליקציה), ניווט מקלדת, וקישור "דלגו לתוכן הראשי" (verified: index.html:625-626 — CSS `.ent-skip-link`; index.html:5997-5998 — השימוש בפועל). מצוין כפריט פתוח בהצהרת הנגישות עצמה. | `[assumed: תואם את הרמה הקיימת באפליקציה הראשית — if wrong: להזמין ביקורת מקצועית]` |
| L10 | שם קובץ: `landing.html` בשורש ה-repo (ליד `index.html`). נתיב גישה: `homey-app-one.vercel.app/landing.html` (ואם תרצה נתיב יפה יותר כמו `/start`, זה rewrite קטן ב-`vercel.json` — לא נדרש עכשיו, מצוין כשלב אופציונלי בסוף). | Executor's latitude |

---

## 2. מבנה הדף (הסקשיינים)

סדר קבוע, כל אחד עם תוכן מוכן (סעיף 3):

1. **Hero** — כותרת ראשית, תת-כותרת, שני כפתורי CTA, צילום מסך של הדשבורד.
2. **הבעיה** — למה כסף משפחתי מפוזר בין וואטסאפ/אקסל/אפליקציית בנק זה כואב.
3. **מה זה Homey** — פתרון אחד, כל הכסף של המשפחה במקום אחד.
4. **תכונות עיקריות** — רשת של 6-7 כרטיסים (אייקון + כותרת + משפט), כמה מהם עם צילום מסך תומך.
5. **חינם מול פרימיום** — טבלת השוואה עם מספרים אמיתיים, פרימיום מתויג "בקרוב".
6. **איך זה עובד** — 3 שלבים.
7. **CTA חוזר** — "נסו עכשיו, בלי התחייבות".
8. **שאלות נפוצות** — 5 שאלות.
9. **פוטר** — קרדיט עסקי, גילוי נאות, הצהרת נגישות, קישור חזרה לאפליקציה.

---

## 3. תוכן מוכן (טיוטת קופי, עברית)

### Hero
- כותרת: **"כל הכסף של המשפחה, במקום אחד"**
- תת-כותרת: "עו״ש, חסכונות, הלוואות, הוצאות וגם הכסף של הילדים — תמונה אחת ברורה, בלי טבלאות אקסל ובלי לנחש איפה עמדתם החודש."
- כפתור ראשי: **"הצטרפו בחינם"** → `<a href="index.html">`
- כפתור משני: **"נסו בלי הרשמה"** → `<a href="index.html?demo=parent">`
- תמונה: צילום מסך של הדשבורד הראשי (מצב דמו)

### הבעיה
כותרת: **"למה זה תמיד מרגיש מבולגן?"**
טקסט: "ההוצאות באפליקציית הבנק, החיסכון בפיקדון אחר, ההלוואה במסמך PDF ששכחתם איפה שמרתם, ודמי הכיס של הילדים — בראש. כל אחד מהם לבד זה בסדר. יחד, קשה לדעת איפה המשפחה עומדת ברגע נתון, ועוד יותר קשה לתכנן קדימה."

### מה זה Homey
כותרת: **"Homey נותן למשפחה תמונה אחת, מעודכנת, ומשותפת"**
טקסט: "כל בן משפחה נכנס עם החשבון שלו, רואה את מה שרלוונטי אליו, ותורם לתמונה הכוללת. הורים רואים תחזית חודשית, חסכונות, הלוואות ושווי נטו. ילדים מקבלים מסך משלהם שמלמד אותם לנהל כסף בעצמם."

### תכונות עיקריות (כרטיסים)
1. **תחזית חודשית חכמה** — "הדשבורד מחשב כמה נשאר לכם להוציא החודש, בזמן אמת, לפי הכנסה, הוצאות והוראות קבע."
2. **עו״ש, חסכונות והשקעות במקום אחד** — "מזינים כמה יש בבנק, כמה בחיסכון וכמה בהשקעות — Homey מחשב שווי נטו אחד, מעודכן."
3. **הלוואות עם סימולטור פירעון מוקדם** — "רואים בדיוק כמה נשאר לשלם, וכמה זמן וכסף אפשר לחסוך בפירעון מוקדם."
4. **הכסף של הילדים** — "מסך נפרד לכל ילד: דמי כיס, יעדי חיסכון, ולמידה פיננסית בגובה העיניים."
5. **סקירה שבועית חכמה (AI)** — "כל שבוע, תובנה קצרה וברורה על ההוצאות שלכם — לא עוד דוח שאף אחד לא קורא." *(פרימיום)*
6. **ריבוי מטבעות** — "חשבון בדולר, נכס באירו — הכל מתורגם למטבע הראשי שלכם אוטומטית." *(פרימיום)*
7. **פרטיות מלאה** — "כל משפחה רואה רק את הנתונים שלה. שום משפחה אחרת לא יכולה לגעת בנתונים שלכם."

### חינם מול פרימיום
כותרת: **"מתחילים בחינם, בלי כרטיס אשראי"**
טבלה:

| | חינם | פרימיום |
|---|---|---|
| הוצאות והכנסות | עד 40 רשומות בחודש | ללא הגבלה |
| הוראות קבע והלוואות | עד הלוואה אחת פעילה | ללא הגבלה |
| חסכונות והשקעות | — | ✓ |
| עיצובים | 2 ערכות עיצוב | כל הערכות |
| סקירה שבועית AI | — | ✓ |
| ריבוי מטבעות | — | ✓ |
| מחיר | חינם לצמיתות | בקרוב |

הערת שוליים: "פרימיום ייפתח לתשלום בקרוב. הצטרפו עכשיו בחינם כדי להתחיל לעקוב אחרי הכסף של המשפחה כבר היום." *(שינוי אחרי ביקורת: הניסוח המקורי "תקבלו עדכון כשזה יעלה" רומז למנגנון התראה יזום שלא קיים באפליקציה כרגע — הוסר. אם בעתיד תרצה מנגנון כזה, זו תוספת נפרדת.)*

### איך זה עובד
1. **נרשמים בדקה, בלי כרטיס אשראי.**
2. **מזינים את המצב הפיננסי שלכם** — עו״ש, חסכונות, הלוואות. Homey בונה לכם תמונה ראשונית מיד.
3. **עוקבים חודש אחרי חודש** — הוצאות, תחזית, יעדים — הכל מתעדכן לבד.

### CTA חוזר
כותרת: **"תנו ל-Homey לסדר לכם את התמונה"**
כפתור: "הצטרפו בחינם" / "נסו בלי הרשמה"

### שאלות נפוצות
1. **זה בטוח?** — "כן. כל משפחה רואה רק את הנתונים שלה, וההרשמה מוגנת בסיסמה."
2. **כמה זה עולה?** — "השימוש הבסיסי חינמי לצמיתות. מנוי פרימיום ייפתח בקרוב."
3. **אפשר להוסיף בני משפחה נוספים?** — "כן — כל משפחה מקבלת קוד קבוצה לשיתוף עם בני המשפחה."
4. **יש גרסה לילדים?** — "כן, לכל ילד יש מסך משלו עם יעדי חיסכון ודמי כיס, בלי גישה לנתונים הכספיים של ההורים."
5. **חייבים כרטיס אשראי כדי להתחיל?** — "לא. ההרשמה לתוכנית החינמית לא דורשת אמצעי תשלום."

### פוטר
פסקה ראשונה (טקסט רגיל): "Homey פותחה על ידי איתמר הורביץ (ENT code, video & beyond). השירות בשלבי הקמה ראשוניים; פרטי עוסק מלאים יעודכנו כאן עם השלמת הרישום."

פסקה שנייה (טקסט רגיל): "הצהרת נגישות: העמוד נבנה בהתאם לעקרונות נגישות בסיסיים; ביקורת נגישות מקיפה טרם בוצעה."

פסקה שלישית — **חלק מהטקסט עטוף בקישור אמיתי**, לא סוגריים מרובעים: "תנאי השימוש ומדיניות הפרטיות המלאים זמינים ⁠" + `<a href="index.html">במסך ההרשמה באפליקציה</a>` + "."

---

## 4. יישום טכני

### 4.1 קובץ חדש: `landing.html`
- HTML סטטי טהור, בלי React/build — כמו שאר האתר, **אבל עם CSS עצמאי משלו** (ראו L6 — לא מעתיקים קלאסים כמו `ent-card`/`ent-pill-btn`, הם תלויים בגיליון Tailwind שחי רק בתוך `index.html`).
- `<style>` inline ב-`landing.html` עצמו, כותב מאפס, בעזרת ערכי הטוקנים הבאים (הכל verified מ-`index.html`, לא להמציא ערכים נוספים):
  - `--navy: #0A0D13; --navy-panel: #10141D; --navy-panel-2: #151A24;` (index.html:425)
  - `--bronze: #CBA05A; --cyan: #4FD8FF; --line: rgba(203,160,90,.16);` (index.html:426)
  - `--font-ui: 'IBM Plex Sans Hebrew', 'Segoe UI', system-ui, sans-serif;` וטקסט לבן/אפור בניגודיות גבוהה על רקע `--navy` (index.html:784 לרדיוסים: `--r-sm:10px; --r-md:14px; --r-lg:20px;`)
  - מבנה מומלץ: `body{background:var(--navy);color:#fff;font-family:var(--font-ui)}`, כרטיסים עם `background:var(--navy-panel);border:1px solid var(--line);border-radius:var(--r-lg)`, כפתור ראשי `background:var(--bronze);color:#1a1206;border-radius:999px`, כפתור משני `border:1px solid var(--line);background:transparent;color:#fff`.
  - Layout: flexbox/grid רגיל ב-CSS טהור (לא Tailwind) — `max-width:1100px;margin:0 auto` למיכל מרכזי, `display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr))` לרשת התכונות, `@media (max-width:640px)` להתאמות מובייל.
- אותו קישור Google Fonts בפועל (verified: index.html:16): `<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Hebrew:wght@400;500;600&display=swap" rel="stylesheet" />` (עם ה-`preconnect` הזוגי שלפניו).
- `dir="rtl"`, `<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />` (זהה לאפליקציה, verified: index.html:5).
- **אין שום עריכה ל-`index.html` הקיים בתוכנית הזו כלל** (ראו L5) — `landing.html` הוא קובץ נפרד לגמרי, אפס סיכון לאפליקציה הראשית.
- קישורי CTA: `<a href="index.html">` (הרשמה) ו-`<a href="index.html?demo=parent">` (דמו — מנגנון קיים, verified: index.html:4239-4256).
- `<link rel="icon">`/`apple-touch-icon` זהים לאפליקציה (קבצים קיימים: `icon-512.png`, `apple-touch-icon.png`).
- `<title>Homey — כל הכסף של המשפחה, במקום אחד</title>`, meta description תואמת ל-hero.

### 4.2 צילומי מסך
כל הכתובות ייפתחו ב-viewport **1280×800** (desktop) פעם, ו-**375×812** (מובייל) פעם נוספת, דרך preview_start בשם "homey" (מוגדר כבר ב-`.claude/launch.json`, משרת סטטי על פורט 5173). לכל כתובת: לחכות לטעינה מלאה, ואז screenshot. לשמור כ-PNG ב-`assets/landing/` (תיקייה חדשה בשורש ה-repo):
1. `index.html?demo=parent&tab=dashboard` → `hero-dashboard.png` — הדשבורד הראשי, למקטע Hero. (אין צורך בלחיצות נוספות.)
2. `index.html?demo=parent&tab=bills` → `feature-loans.png` — לשונית "הוראות קבע והלוואות". **לפני הצילום:** ללחוץ על הכפתור "פירוט וסימולטור פירעון מוקדם" בתוך כרטיס ההלוואה הראשון כדי לפתוח את הפירוט, אחרת הסימולטור לא נראה.
3. `index.html?demo=parent&tab=dashboard` → `feature-bank.png` — לגלול/לצלם את אזור כרטיס "עובר ושב" ורשת 4 האריחים (יתרה חודשית/חסכונות/נכסים/שווי נטו), בראש הדשבורד.
4. `index.html?demo=kid&tab=dashboard` → `feature-kid.png` — מסך "הכסף שלי" של פרופיל ילד (KidDashboard).
גודל קובץ לא קריטי בשלב זה (אין עדיין תעבורה אמיתית לדף) — לא נדרשת דחיסה מיוחדת, רק PNG רגיל מה-screenshot tool.

### 4.3 נגישות בסיסית (יישום, לא רק הצהרה)
- כל `<img>` מקבל `alt` תיאורי (לא ריק).
- מבנה כותרות היררכי: `h1` יחיד ב-Hero, `h2` לכל סקשן, בלי דילוג רמות.
- כפתורי CTA הם `<a>`/`<button>` אמיתיים (לא `<div onclick>`), עם ניגודיות שכבר אומתה בטוקנים הקיימים של ה-theme.
- `skip to content` link בראש הדף (אותו דפוס CSS כמו באפליקציה עצמה, verified: index.html:625-626 — `.ent-skip-link { position:fixed; top:-60px; ...; }` + `:focus{top:12px}`; שימוש בפועל ב-index.html:5997-5998, "דלגו לתוכן הראשי").

---

## 5. סדר בנייה (Build Phases)

1. **צילומי מסך** (סעיף 4.2) — לתפוס קודם, כדי שיהיו זמינים בזמן בניית הדף. שמירה ל-`assets/landing/`.
2. **`landing.html`** — מבנה + CSS עצמאי (סעיף 4.1) + כל הסקשיינים מסעיף 3, עם התמונות האמיתיות מוטמעות. בדיקת syntax + preview בדפדפן (desktop 1280px + מובייל 375px), שני הכפתורים מנווטים נכון (`index.html` להרשמה, `index.html?demo=parent` לדמו).
3. **מעבר נגישות** (סעיף 4.3) — סריקה ידנית של הדף שנבנה מול הרשימה.
4. **בדיקה סופית בדפדפן:** desktop + מובייל 375px, שני מצבי CTA, כל קישור עובד (כולל הקישור בפוטר), אין שגיאות קונסול.
5. **commit מקומי, לא לדחוף עד אישור מר הורביץ** (כמו תמיד בסשן הזה). **קובץ יחיד חדש (`landing.html`) ותיקיית תמונות חדשה (`assets/landing/`) — `index.html` לא נוגע בכלל.**

---

## 6. Landmines & Adaptations

- **תוקן בזמן בנייה (לא היה צפוי בתכנון):** כלי צילום-המסך של סביבת הבנייה (`computer{action:"screenshot", save_to_disk:true}`) לא כתב בפועל אף קובץ שניתן לאתר בדיסק (נבדק עם Glob/PowerShell אחרי מספר קריאות — אפס קבצים חדשים). במקום לחכות/להתעקש על מנגנון שלא עובד, **הוחלף L4 בזמן אמת**: תמונות ה"תכונות" בעמוד הן שכפולים קטנים ב-HTML/CSS טהור (לא תמונות PNG) של קטעי מסך אמיתיים מהאפליקציה — עם **אותם מספרים ותוויות מדויקים** שנצפו בפועל דרך הדפדפן בזמן הבנייה (לא הומצאו), כדי לשמור על האותנטיות שהייתה מטרת L4 המקורי. זו שיטה תקינה מבחינה טכנית (HTML/CSS תמיד "חד" בכל רזולוציה, בלי קבצי תמונה לנהל), אבל שונה מהתכנון שאושר — מצוין כאן בגלוי כפי שהכלל מחייב.

- **קיצור/PWA קיים ב-`/`:** אם היה נבחר להפוך את `/` לעמוד השיווקי (החלופה שנשקלה ונדחתה, L3), כל מי שכבר משתמש באפליקציה (כולל המשפחה שלך) היה "ננעל בחוץ" מהקיצור השמור שלו. זו הסיבה שהוחלט אחרת מבלי לשאול — הסיכון חד-כיווני וקשה לתקן בדיעבד.
- **קישורי תנאי שימוש/פרטיות:** אינם כתובות URL עצמאיות באפליקציה (React state בלבד) — הפוטר לא "מתחזה" לקשר אליהם ישירות, אלא מפנה למסך ההרשמה שבו הם זמינים בפועל (L7). אם בעתיד תרצה עמודי terms/privacy עצמאיים בכתובת משלהם, זה שינוי נפרד.
- **מצב לא-רשום כעסק:** העמוד לא מציג מחירים או שפה שמרמזת על גבייה פעילה (L1), ולא אוסף מידע אישי כלשהו (L8) — כדי לא ליצור ציפייה או חובת גילוי שהעסק עוד לא ערוך אליה.

---

## 7. אימות סופי (checklist)

- [ ] `landing.html` נטען, כל הסקשיינים מוצגים עם התמונות האמיתיות, שני כפתורי CTA מנווטים נכון (`index.html` / `index.html?demo=parent`).
- [ ] הקישור בפוטר לאפליקציה עובד (לא רק טקסט בסוגריים).
- [ ] בדיקת syntax (vm.Script) על `index.html` — 0 שגיאות (למרות שלא נערך, לוודא שלא נגעו בו בטעות).
- [ ] מובייל 375px — אין גלילה אופקית, טקסט קריא, כפתורים בגודל נגיש למגע.
- [ ] כל התמונות עם `alt`, מבנה כותרות תקין (h1 יחיד, h2 לכל סקשן).
- [ ] אין שגיאות קונסול.
- [ ] `index.html` לא השתנה כלל (git diff ריק על הקובץ הזה).
- [ ] לא נדחף ל-git עד אישור מפורש.
